resources:
  - manifests.yaml
  - service.yaml

configurations:
  - kustomizeconfig.yaml

patches:
  - path: mutating-webhook-namespace-selector.yaml
  - path: validating-webhook-namespace-selector.yaml
